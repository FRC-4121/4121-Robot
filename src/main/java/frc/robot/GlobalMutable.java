package frc.robot;

/*
 * This is the ONLY PLACE that global mutable variables go, and it should be used sparingly.
 * There will be NO `import static`s from here, every use must be explicitly prefixed with a `GlobalMutable`.
 */
public final class GlobalMutable {
  public static boolean killAuto = false;
}
